'use client';

import EmailField from '@repo/ui/components/Fields/EmailField';
import createBasicController from './createBasicController';

const EmailFieldController = createBasicController(EmailField);

export default EmailFieldController;
